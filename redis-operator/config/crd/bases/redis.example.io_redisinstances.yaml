---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: redisinstances.redis.example.io
spec:
  group: redis.example.io
  names:
    kind: RedisInstance
    listKind: RedisInstanceList
    plural: redisinstances
    shortNames:
    - ri
    singular: redisinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RedisInstance is the Schema for the redisinstances API.
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation
              of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this
              object represents.
            type: string
          metadata:
            type: object
          spec:
            description: RedisInstanceSpec defines the desired state of RedisInstance.
            properties:
              auth:
                description: Auth configures Redis authentication.
                properties:
                  passwordSecretRef:
                    description: PasswordSecretRef references a Secret key containing
                      the Redis password.
                    properties:
                      key:
                        description: The key of the secret to select from. Must be
                          a valid secret key.
                        type: string
                      name:
                        description: Name of the referent.
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                  tls:
                    description: TLS configures optional TLS for Redis connections.
                    properties:
                      secretName:
                        description: SecretName is the name of the Secret containing
                          TLS certificates.
                        type: string
                    required:
                    - secretName
                    type: object
                type: object
              config:
                additionalProperties:
                  type: string
                description: Config is a map of redis.conf key-value overrides.
                type: object
              redisVersion:
                description: RedisVersion is the Redis container image reference
                  (e.g. "redis:7.2").
                type: string
              replicas:
                description: Replicas is the total number of Redis pods (1 primary
                  + N-1 replicas).
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources defines CPU and memory requests/limits for
                  Redis pods.
                properties:
                  claims:
                    description: Claims lists the names of resources, defined in
                      spec.resourceClaims, that are used by this container.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: Name must match the name of one entry in pod.spec.resourceClaims
                            of the Pod where this field is used.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Limits describes the maximum amount of compute resources
                      allowed.
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Requests describes the minimum amount of compute
                      resources required.
                    type: object
                type: object
              storage:
                description: Storage defines the PVC template for Redis data volumes.
                properties:
                  accessModes:
                    description: accessModes contains the desired access modes the
                      volume should have.
                    items:
                      type: string
                    type: array
                  dataSource:
                    description: dataSource field can be used to specify either a
                      VolumeSnapshot object or a PVC object.
                    properties:
                      apiGroup:
                        description: APIGroup is the group for the resource being
                          referenced.
                        type: string
                      kind:
                        description: Kind is the type of resource being referenced
                        type: string
                      name:
                        description: Name is the name of resource being referenced
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  dataSourceRef:
                    description: dataSourceRef specifies the object from which to
                      populate the volume with data.
                    properties:
                      apiGroup:
                        description: APIGroup is the group for the resource being
                          referenced.
                        type: string
                      kind:
                        description: Kind is the type of resource being referenced
                        type: string
                      name:
                        description: Name is the name of resource being referenced
                        type: string
                      namespace:
                        description: Namespace is the namespace of resource being
                          referenced
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  resources:
                    description: resources represents the minimum resources the volume
                      should have.
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Limits describes the maximum amount of compute
                          resources allowed.
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Requests describes the minimum amount of compute
                          resources required.
                        type: object
                    type: object
                  selector:
                    description: selector is a label query over volumes to consider
                      for binding.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements.
                        items:
                          description: A label selector requirement is a selector
                            that contains values, a key, and an operator.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: operator represents a key's relationship
                                to a set of values.
                              type: string
                            values:
                              description: values is an array of string values.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: matchLabels is a map of key-value pairs.
                        type: object
                    type: object
                  storageClassName:
                    description: storageClassName is the name of the StorageClass
                      required by the claim.
                    type: string
                  volumeMode:
                    description: volumeMode defines what type of volume is required
                      by the claim.
                    type: string
                  volumeName:
                    description: volumeName is the binding reference to the PersistentVolume
                      backing this claim.
                    type: string
                type: object
              topology:
                default: standalone
                description: 'Topology defines the Redis topology: standalone or
                  sentinel.'
                enum:
                - standalone
                - sentinel
                type: string
            required:
            - redisVersion
            - replicas
            - storage
            - topology
            type: object
          status:
            description: RedisInstanceStatus defines the observed state of RedisInstance.
            properties:
              conditions:
                description: Conditions contains the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              masterEndpoint:
                description: MasterEndpoint is the DNS name of the current writable
                  primary.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest metadata.generation
                  reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase of the RedisInstance.
                type: string
              readyReplicas:
                description: ReadyReplicas is the count of ready Redis replicas.
                format: int32
                type: integer
              sentinelEndpoints:
                description: SentinelEndpoints contains DNS addresses of Sentinel
                  instances.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
